# leBonMur

##Installation

```
composer install
yarn
```

##Development

```
symfony server:start -d
docker-compose up -d
yarn run dev-server
symfony console make:migrations
symfony console doctrine:fixtures:load
```
