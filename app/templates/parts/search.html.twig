<div class="card">
    <div class="card-header"><p>Search</p> </div>
    <div class="card-body">
        {{form_start(searchForm)}}
        {{form_widget(searchForm)}}
        {{form_end(searchForm)}}
    </div>
</div>
